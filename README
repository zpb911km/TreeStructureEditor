instant有基于pyqt6的界面，老好用了
qss里边还可以改风格
## 使用方法
安装对应库后运行display.py即可
## 注意事项
1. 如果要使用pyqt6的界面,需要安装pyqt6
2. 第一行是顶格的,其它不是也不能
3. 每次编辑必须选择路径,切换文件时最好关掉重启(这个bug会修好的)
4. 底栏报错不太详细,懒得搞
5. 仅支持纯文本,推荐先生成文本,再到word里边修饰